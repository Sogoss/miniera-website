# Refuse to install with the wrong Node or npm instead of silently rewriting
# package-lock.json in a different format. npm 11 and npm 10 disagree about the
# `libc` fields, and the difference only surfaces as a few hundred lines of
# diff noise on somebody else's machine.
engine-strict=true
